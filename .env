# Environment Variables for LiveRooms Project

# Frontend Configuration
# Signalling Server URL - used by the frontend to connect to the WebRTC signalling server
# For development: http://localhost:3000
# For production: your production server URL
# For Docker: http://server:3000 (uses service name from docker-compose)
VITE_SIGNALLING_SERVER_URL=http://localhost:3000

# Server Configuration
# Port for the signalling server (default: 3000)
PORT=3000