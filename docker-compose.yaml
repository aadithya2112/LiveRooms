services:
  server:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.server
    ports:
      - "3001:3000"
    container_name: "live-rooms-server"
    networks:
      - live-rooms-network
  
  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.web
      args:
        VITE_SIGNALLING_SERVER_URL: https://roomsnew-ss.aadithya.tech
    ports:
      - "5173:5173"
    container_name: "live-rooms-web"
    depends_on:
      - server
    networks:
      - live-rooms-network

networks:
  live-rooms-network:
    driver: bridge